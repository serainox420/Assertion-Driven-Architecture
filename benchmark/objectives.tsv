# ADA benchmark objectives.  Columns: LEVEL <tab> MAX_STEPS <tab> OBJECTIVE
# Lines starting with '#' are ignored.  scripts/bench.sh reads this file;
# benchmark/expected.md documents the expected output per level.
L1	8	create the file /tmp/ada-bench/ready and prove it exists
L2	10	write the kernel release (output of 'uname -r') into /tmp/ada-bench/kernel.txt and prove the file exists and is non-empty
L3	12	identify the current user and OS and save a short report to /tmp/ada-bench/env.txt, then prove the file exists
L4	12	create /tmp/ada-bench/app/config.yaml containing the line 'mode: prod' with file mode 0644, and prove it exists with that mode
L5	16	start a background HTTP server on port 8085 serving /tmp/ada-bench and prove the port is listening (python3 is available)
L6	14	verify that jq is installed, capture 'jq --version' into /tmp/ada-bench/jq.txt, and prove that file exists
L7	16	using a single sentinel check, validate that /tmp/ada-bench/app/config.yaml exists and is non-empty, and prove success
L8	20	ensure /tmp/ada-bench/daemon.pid points to a live process; if it does not, start a 'sleep 600' background daemon, record its PID to that file, then prove the process is running
